{
  "name": "king-nofap",
  "version": "1.0.1",
  "private": true,
  "description": "King — habit journey tracker",
  "scripts": {
    "web:copy": "node scripts/copy-web.js",
    "cap:sync": "npm run web:copy && npx cap sync",
    "cap:android": "npm run cap:sync && npx cap open android",
    "cap:ios": "npm run cap:sync && npx cap open ios",
    "android:debug": "npm run web:copy && cd android && ./gradlew :app:assembleDebug",
    "android:install": "npm run android:debug && node scripts/install-debug-apk.js",
    "android:bundle": "npm run web:copy && cd android && ./gradlew :app:bundleRelease",
    "icons:generate": "python3 scripts/generate-icons.py",
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "review:zip": "bash scripts/make-review-zip.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fedmudit-max/Laotzu.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/fedmudit-max/Laotzu/issues"
  },
  "homepage": "https://github.com/fedmudit-max/Laotzu#readme",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/share": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
